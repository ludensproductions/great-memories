#!/usr/bin/env bash

if [[ "$GREAT_MEMORIES_ENV" == "production" ]]; then
  echo "This command can only be run in development environments"
  exit 1
fi

cd /usr/src/app || exit
pnpm --filter great-memories exec nest start --debug "0.0.0.0:9230" --watch -- "$@"
